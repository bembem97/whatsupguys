import Editor from "@shared/utilities/Editor"
import React from "react"
import sandpackProps from "."

const TrashedIntro: React.FC = () => {
    return <Editor {...sandpackProps} />
}

export default TrashedIntro
